/**
 * Main canvas: column flex, fills height beside sidebar, caps width, no left gap.
 * Parent main column uses items-start so this stays left-aligned under the divider.
 */
export const dashboardCanvasClass =
  'flex min-h-0 w-full max-w-[1160px] flex-1 flex-col overflow-hidden pl-0 pr-3 sm:pr-4 lg:pr-5'

/** Header vertical rhythm */
export const headerPaddingY = 'py-3.5'

/** Space between header and first page row */
export const pagePaddingTop = 'pt-4'

/** Page bottom padding */
export const pagePaddingBottom = 'pb-5'
